/* Single source of truth for the shipped build. Loaded by the page (title
 * screen stamp) and by sw.js via importScripts (cache name), so the version
 * shown in the menu always matches the cache the page was served from.
 * Bump this to ship an update. */
const GAME_VERSION = 'v18';
